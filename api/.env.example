# Postgres (docker-compose host port)
PG_HOST=localhost
PG_PORT=25432
PG_USERNAME=zerohcom
PG_PASSWORD=zerohcom_dev

# Mongo (docker-compose host port)
MONGO_HOST=localhost
MONGO_PORT=27027
MONGO_USERNAME=zerohcom
MONGO_PASSWORD=zerohcom_dev

# JWT signing secret (override in production!)
JWT_SECRET=replace-me-with-a-long-random-string

# CORS — comma-separated origins
CORS_ALLOWED_ORIGINS=http://localhost:25173
